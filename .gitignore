# --- data & artifacts kept OUT of the repo -------------------------------
# ContractNLI (CC BY 4.0) is fetched locally, not redistributed here.
data/official/
vendor/
# Frozen manifests embed contract-clause text -> regenerate from the seed instead.
data/manifests/*.json
experiments/semantic_receptor_rq2/manifests/*.json
data/micro_contracts.json
# Run outputs, caches (model I/O), logs.
outputs/
*.log

# Private benchmark checkouts, generated evidence, and machine-local runtime state.
data/host_boundary_v2/
data/.embeddings_cache/
experiments/**/runtime_envs/
experiments/**/runs/
experiments/**/start-intents/
experiments/**/live_campaign*/
experiments/**/prepared_live_run*/
experiments/**/private*/
experiments/**/route*/
experiments/**/auth*/
experiments/**/source_cache/
experiments/**/.source_cache/

# Generated databases, columnar data, archives, wheels, and native libraries.
*.db
*.db-*
*.sqlite
*.sqlite3
*.sqlite3-*
*.parquet
*.arrow
*.tar
*.tar.gz
*.tgz
*.zip
*.whl
*.so
*.dylib
*.bundle

# --- python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/
.env
.env.*

# --- os -------------------------------------------------------------------
.DS_Store
